# URL del backend (cuando exista). Con mocks activos no se usa.
VITE_API_URL=http://localhost:8000
# false = llamadas reales; cualquier otro valor = mocks con data dummy en el frontend
VITE_USE_MOCK=true
